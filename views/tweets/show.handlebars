<h1>Show page</h1>

{{tweet.message}} - {{tweet.user.name}}

<h3>Write a comment</h3>
<form method="POST" action="/tweets/comments/{{tweet._id}}">
    <textarea name="comment"></textarea>
    <input type="submit" value="Post"/>
</form>

<ul>
    {{#each tweet.comments}}
    <li>{{this.comment}}</li>
    {{/each}}
</ul>

<h3>Do you want to edit this tweet?</h3>
<form method="POST" action="/tweets/{{id}}?_method=PUT">
    <div>
        <label for="tweet">Tweet</label>
        <input type="text" name="message" id="tweet"/>
    </div>
    <input type="submit" value="Edit"/>
</form>

<form method="POST" action="/tweets/{{id}}?_method=DELETE">
    <label>Do you want to delete this tweet?</label>
    <input type="submit" value="Delete"/>
</form>